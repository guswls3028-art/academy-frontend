/** UUID v4 with the same secure entropy on browsers predating randomUUID(). */
export function createRandomUuid(): string {
  if (typeof crypto !== "undefined" && typeof crypto.randomUUID === "function") {
    return crypto.randomUUID();
  }
  const bytes = new Uint8Array(16);
  // Do not substitute Math.random(): these IDs also deduplicate saved mutations.
  crypto.getRandomValues(bytes);
  bytes[6] = (bytes[6] & 0x0f) | 0x40;
  bytes[8] = (bytes[8] & 0x3f) | 0x80;
  let hex = "";
  for (let index = 0; index < bytes.length; index++) {
    hex += (`0${bytes[index].toString(16)}`).slice(-2);
  }
  return `${hex.slice(0, 8)}-${hex.slice(8, 12)}-${hex.slice(12, 16)}-${hex.slice(16, 20)}-${hex.slice(20)}`;
}
