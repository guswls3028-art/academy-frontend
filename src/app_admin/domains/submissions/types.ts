// PATH: src/app_admin/domains/submissions/types.ts
// Compatibility facade. Canonical submission contract lives in shared.
export type {
  Submission,
  SubmissionJsonRecord,
  SubmissionSource,
  SubmissionStatus,
  SubmissionTargetType,
} from "@/shared/api/contracts/submissions";
