# -----------------------------------------------------------------------------
# Node / 패키지 (pnpm 전용 — npm 사용 금지)
# -----------------------------------------------------------------------------
node_modules
.pnpm-store
package-lock.json
dist
build
*.tsbuildinfo

# -----------------------------------------------------------------------------
# 환경 변수 (비밀값·로컬 오버라이드 — 커밋 금지)
# -----------------------------------------------------------------------------
.env
.env.*
!.env.example
!.env.development
!.env.production

# -----------------------------------------------------------------------------
# 로그 / 캐시 / 임시
# -----------------------------------------------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.tmp
.cache
.vite

# -----------------------------------------------------------------------------
# OS
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db

# -----------------------------------------------------------------------------
# IDE / 에디터
# -----------------------------------------------------------------------------
.vscode
.idea
*.sublime-*
*~
*.swp
*.swo

# -----------------------------------------------------------------------------
# 테스트 / 커버리지
# -----------------------------------------------------------------------------
coverage

# -----------------------------------------------------------------------------
# 배포 플랫폼 로컬 아티팩트 (선택)
# -----------------------------------------------------------------------------
.vercel
.netlify

# -----------------------------------------------------------------------------
# 프로젝트 규칙: 작업용·프롬프트·일반 스펙 파일 제외 (SSOT 예외만 허용)
# -----------------------------------------------------------------------------
*프롬프트.ts
*작업용*.ts
*스펙.ts
*spec.ts
!e2e/**/*.spec.ts
*prompt.ts
docs/
prompts/
specs/
temp/
# 빌드에 필요한 도메인 SSOT만 예외
!src/shared/ui/domain/tableColumnSpec.ts

# 로컬 테스트 자료 (git 제외)
로컬테스트자료/

# E2E test artifacts
test-results/
playwright-report/
e2e/.auth/
e2e/screenshots/
e2e/reports/
# exam-pdf-extract E2E가 런타임에 생성
e2e/fixtures/test-exam.pdf
.env.e2e
.env.local

# 로컬 디버그·리뷰 스크립트·스크린샷 (공유 불필요)
e2e/_local/
_e2e_review_screenshots/
e2e/audit/_uiux_review/

# 로컬 빌드 산출물 (dist 외 변형)
dist-final/

# 임시 파일 (tmp_<pid>_<hash>)
_tmp_*

# 에이전트/IDE 워크스페이스 아티팩트
.cursor/

# Local-only directories
node_modules_win/
.wrangler/
